# Deployment target: Cloudflare Workers + D1
#
# Local development:
#   pnpm install
#   cp .dev.vars.example .dev.vars   # fill in secrets
#   pnpm wrangler d1 create goventurevalue-db   # once — paste database_id into wrangler.jsonc
#   pnpm db:migrate:local
#   pnpm dev
#
# Production deploy:
#   pnpm db:migrate:remote
#   pnpm deploy
#   https://goventurevalue.devshilov.workers.dev
